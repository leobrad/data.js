import { describe, expect, test, } from '@jest/globals';
import Users from '~/class/table/Users';
import global from '~/obj/global';

beforeAll(() => {
});

describe('[class] Users clear records cache test case;', () => {
});

afterAll(async () => {
});
